
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Filmes {
  id String @id @default(uuid())
  name String
  director String
  synopsis String
  gender String
  releaser Int
}